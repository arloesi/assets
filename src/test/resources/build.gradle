
apply plugin:"assets"

buildscript {
  repositories {
    mavenCentral()

    maven {
      url "file://Users/david/Workspaces/Vertex/maven"
    }
  }

  dependencies {
    classpath group:"arloesi",name:"assets",version:"0.1"
  }
}

assets {
  optimize = true
  target = "assets"
}
