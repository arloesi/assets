bundles {
  common {
    styles = ["common","layout"]
    scripts= ["common","utility"]
    images = ["assets/images"]
    markup = ["common"]
  }
}

modules {
  main {
    include = ["common"]
    scripts = ["main"]
    styles = ["main"]
    markup = ["main"]
  }

  login {
    scripts = ["login"]
    styles = ["login"]
    markup = ["login"]
  }
}