bundles {
  common {
    styles = ["common","layout"]
    scripts= ["common","utility"]
    images = ["assets/images"]
    markup = ["common"]
  }
}

modules {
  home {
    include = ["common"]
    scripts = ["home"]
    styles = ["home"]
    markup = ["home"]
  }

  login {
    scripts = ["login"]
    styles = ["login"]
    markup = ["login"]
  }
}