bundles {
  common {
    styles = ["common","layout"]
    scripts= ["common","utility"]
    markup = ["common"]

    images {
      styles.images = ["library/jquery-ui/css/ui-lightness/images"]
    }
  }
}

modules {
  home {
    include = ["common"]
    scripts = ["home"]
    styles = ["home"]
    markup = ["home"]
  }

  login {
    scripts = ["login"]
    styles = ["login"]
    markup = ["login"]
  }
}